

    var facts = bone.get('facts');
    
    var $list = $('

    facts.io.route('facts', {
        results: function() {
            $list.html('');
            _.each(
                
        }
    });

    bone.start()
